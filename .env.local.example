# URL of the live Spring Boot backend (no trailing slash).
# Requests to /api/* on this app are proxied server-side to this URL —
# see next.config.js for why (the backend has no CORS config).
BACKEND_URL=http://16.170.244.112:8080
